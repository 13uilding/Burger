<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{this.id}}" data-newstate="{{eaten}}">
		{{#if eaten}}EXTRICATE!{{else}}EAT ME!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
